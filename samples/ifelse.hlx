import console

// if / else demo

function max(a, b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

let x = 10;
let y = 20;
let result = max(x, y);
console.print(result);
